<Window x:Class="DotNetBuilder.Views.CommitMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:AduSkin="clr-namespace:AduSkin.Controls;assembly=AduSkin"
        Title="输入提交信息"
        Width="500"
        Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 项目信息 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="{Binding ProjectName, StringFormat='项目: {0}'}"
                       FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding ChangesCount, StringFormat='有 {0} 个文件需要提交'}"
                       Foreground="#666" FontSize="12"/>
        </StackPanel>

        <!-- 提示 -->
        <TextBlock Grid.Row="1" Text="请输入提交信息（留空则跳过提交直接同步）:"
                   Margin="0,0,0,10" Foreground="#333"/>

        <!-- 输入框 -->
        <AduSkin:AduTextBox x:Name="CommitMessageBox"
                           Grid.Row="2"
                           Text="{Binding CommitMessage, UpdateSourceTrigger=PropertyChanged}"
                           TextWrapping="Wrap"
                           AcceptsReturn="True"
                           VerticalScrollBarVisibility="Auto"
                           ToolTip="输入提交信息..."
                           Margin="0,0,0,10"/>

        <!-- 按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <AduSkin:AduButton x:Name="BtnSkip"
                             Content="跳过提交"
                             Width="100"
                             Margin="0,0,10,0"
                             Click="BtnSkip_Click"/>
            <AduSkin:AduButton x:Name="BtnOK"
                             Content="确认提交"
                             Width="100"
                             Margin="0,0,10,0"
                             Click="BtnOK_Click"
                             Style="{StaticResource ButtonPrimary}"/>
            <AduSkin:AduButton x:Name="BtnCancel"
                             Content="取消"
                             Width="100"
                             Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
